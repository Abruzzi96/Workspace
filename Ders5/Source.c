#include <stdio.h>

//float calculateAvg(int array[]) {
//	int toplam = 0;
//	for (int i = 0; i < 5; i++) {
//		toplam += array[i];
//	}
//
//	return toplam / 5.0;
//}


int main() {
	
	// int data[100];
		// int x;

	//int x = 5;

	//int marks[] = { 12,56,65,21,74 };

	//for (int i = 0; i < 5; i++) {
	//	printf("%d ", marks[i]);
	//}

	/*int data[3];

	for (int i = 0; i < 3; i++) {
		printf("data[%d] = ", i);
		scanf_s("%d", &data[i]);
	}

	printf("\n\n================ARRAY================\n\n");

	for (int i = 0; i < 3; i++) {
		printf("%d ", data[i]);
	}*/

	/*
	
			Define an array and let user to fill it.
			Find the max and min elements in that array and print them.
	
	*/


	//int array[5]; 

	//for (int i = 0; i < 5; i++) {
	//	printf("array[%d]: ",i);
	//	scanf_s("%d", &array[i]);
	//}

	//int toplam = 0;
	//for (int i = 0; i < 5; i++) {
	//	toplam += array[i];
	//}

	//float ort = 0;
	//ort = toplam / 5.0;
	//printf("%f", ort);

	/*int array[5];

	for (int i = 0; i < 5; i++) {
		printf("array[%d]: ",i);
		scanf_s("%d", &array[i]);
	}
	int max = array[0];
	int min = array[0];
	for (int i = 0; i < 5; i++) {
		if (array[i] > max) {
			max = array[i];
		}
		if (array[i] < min) {
			min = array[i];
		}
	}
	printf("Max = %d\nMin = %d", max, min);*/

	//int A[3][3];
	//int B[3][3];
	//int C[3][3];

	//for (int row = 0; row < 3; row++) {
	//	for (int col = 0; col < 3; col++) {
	//		printf("A[%d][%d] = ", row, col); // A[0][0]
	//		scanf_s("%d", &A[row][col]);
	//	}
	//}

	//printf("\n\n");

	//for (int row = 0; row < 3; row++) {
	//	for (int col = 0; col < 3; col++) {
	//		printf("B[%d][%d] = ", row, col); // A[0][0]
	//		scanf_s("%d", &B[row][col]);
	//	}
	//}

	//for (int row = 0; row < 3; row++) {
	//	for (int col = 0; col < 3; col++) {
	//		C[row][col] = A[row][col] * B[row][col];
	//	}
	//}

	//printf("\n\n");


	//for (int row = 0; row < 3; row++) {
	//	for (int col = 0; col < 3; col++) {
	//		printf("C[%d][%d] = %d\n", row, col, C[row][col]);
	//	}
	//}



	/*int array[5]; 

	for (int i = 0; i < 5; i++) {
		printf("array[%d]: ",i);
		scanf_s("%d", &array[i]);
	}

	
	printf("%f", calculateAvg(array));*/

	return 0;
}